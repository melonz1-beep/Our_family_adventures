(()=>{'use strict';const RELEASE='10.3.14';let correcting=false;function setTitle(){if(correcting)return;const expected=`Our Family Adventures ${RELEASE}`;if(document.title!==expected){correcting=true;document.title=expected;correcting=false}}function setStoredVersion(){try{const key='ofa-9-data',value=localStorage.getItem(key);if(value){const data=JSON.parse(value);data.version=RELEASE;localStorage.setItem(key,JSON.stringify(data))}}catch(error){console.warn('Version storage update',error)}}function start(){setTitle();setStoredVersion();const title=document.querySelector('title');if(title){const observer=new MutationObserver(setTitle);observer.observe(title,{childList:true,characterData:true,subtree:true});window.OFAVersionTitleGuard=observer}window.OFAVersion=RELEASE}if('serviceWorker'in navigator)navigator.serviceWorker.getRegistrations().then(rows=>rows.forEach(registration=>{registration.update().catch(()=>{});registration.waiting?.postMessage({type:'SKIP_WAITING'});registration.active?.postMessage({type:'CLEAR_OLD_CACHES'})})).catch(()=>{});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start()})();