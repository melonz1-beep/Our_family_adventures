rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This app stores family data in Realtime Database, not Firestore.
    // Deny the unused service so a signed-in but unapproved account cannot use it.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
