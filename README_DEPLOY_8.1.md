# Our Family Adventures 8.1 Deploy

Upload all files and folders in this package to the repo root and commit.

8.1 adds: happy hour/dining out meals, open meal/grocery/packing acceptance, group/private trip visibility, named RSVP/proposals, in-app weather panel, itinerary website links, media library, scrapbook undo, more backgrounds/cutout shapes, and updated PWA cache.

After deployment, refresh twice or reopen the installed app so the new service worker cache `ofa-8-1-0` activates.
