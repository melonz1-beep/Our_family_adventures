# Final 6.4.3 visual asset fix

- Replaced the accidentally saved screenshot asset with a clean home/splash image.
- Removed the embedded “Begin Our Journey” button from the image asset so the app does not look like it has two buttons.
- Removed the giant notification bar from the image assets.
- Added stronger CSS overrides so notification and chat controls stay compact.
- Restored a compact floating chat button.
