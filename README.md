# Our Family Adventures 4.3 — Lifelike App Icon Update

Upload everything inside this folder to your GitHub Pages repository root.

This build keeps the 4.2 app and updates the app branding assets:

- Lifelike lighthouse splash background
- Lifelike lighthouse home background
- Photorealistic lighthouse app icons for install/home screen
- Favicon added
- Service worker cache bumped to 4.3
- Manifest start URL updated to `?v=43`

After upload, open your site with `?v=43` to force the newest version.
