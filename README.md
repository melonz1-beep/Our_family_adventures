# Project Lighthouse 3.4 – Notifications Preview

Upload all files and folders in this build to GitHub Pages.

This build adds a working in-app notification center and optional browser notification preview. True push notifications when the app is closed will require Firebase Authentication and Firebase Cloud Messaging in a future production build.
