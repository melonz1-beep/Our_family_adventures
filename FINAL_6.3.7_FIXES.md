# Version 6.3.7 Fixes

Includes per-link voting, vote change support, private meal/grocery/packing views, group assignment dropdowns, notification center with unread count and mark-read/clear controls, draggable floating chat, profile login/edit/photo controls, weather and map fallbacks, map history pins, page-start navigation, and expanded scrapbook themes/frames/stickers.

Note: the Google Photos link for the requested splash/home image could not be embedded directly from the shared URL. Download the image from Google Photos and replace `assets/splash-background.png` and `assets/lighthouse-home.png` with that file using the same names.
