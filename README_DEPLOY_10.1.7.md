# Our Family Adventures 10.1.8

Fixes scrapbook editor stability, adds owner/Admin scrapbook deletion, repairs full-page scrapbook viewing, and places trip voting directly on each dedicated trip page above lists and RSVPs. Voting links are normalized so links entered without https:// still open correctly.

Deploy every file in this package. Then open `?v=10.1.8`. No Firebase rules or Cloud Functions update is required.
