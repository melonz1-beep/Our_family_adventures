# Our Family Adventures 9.0.6

Fixes the mobile scrapbook editor so controls and photo-selection buttons remain accessible and the canvas stays in normal page flow without covering the editor or finalized pages. Adds a visible Add Checked Photos button above and below the thumbnail picker. Voting now removes all aliases of the current user's previous vote within the same category before saving the new option, and includes a separate Remove My Vote control.
