# Our Family Adventures 10.1.8

Fixes included:
- Scrapbook trip dropdown starts with **Choose a trip** and does not automatically select a trip.
- The scrapbook editor asks the user to choose a trip before opening.
- Completed scrapbook pages scale the entire portrait or landscape canvas into the full-page viewer.
- Dedicated trip pages show the trip name, dates, and destination before maps and weather.
- Trip voting appears immediately after trip details and before maps, lists, RSVPs, and other trip modules.

Deploy all files together, then refresh with `?v=10.1.8`.
