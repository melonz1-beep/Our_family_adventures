(()=>{'use strict';const RELEASE='10.3.16';let correcting=false;function setTitle(){if(correcting)return;const expected=`Our Family Adventures ${RELEASE}`;if(document.title!==expected){correcting=true;document.title=expected;correcting=false}}function start(){setTitle();try{const key='ofa-9-data',raw=localStorage.getItem(key);if(raw){const data=JSON.parse(raw);data.version=RELEASE;localStorage.setItem(key,JSON.stringify(data))}}catch(e){console.warn('Version update',e)}const title=document.querySelector('title');if(title)new MutationObserver(setTitle).observe(title,{childList:true,characterData:true,subtree:true});window.OFAVersion=RELEASE}if('serviceWorker'in navigator)navigator.serviceWorker.getRegistrations().then(rows=>rows.forEach(reg=>{reg.update().catch(()=>{});reg.waiting?.postMessage({type:'SKIP_WAITING'});reg.active?.postMessage({type:'CLEAR_OLD_CACHES'})})).catch(()=>{});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start()})();