# Firebase Security Rules

Use the included `firestore.rules` and `storage.rules` files as the Version 5.0 starting point. Review family roles and invite flow before public launch.

Private adventures should only be visible to invited members, owners, or admins. Storage uploads should require sign-in and limit uploaded content to images, videos, and PDFs.
